# Why RegimeForge Should Win Track 2

## Technical Execution
- Complete end-to-end skill: data → regime → strategy → validate → critique → backtest
- NVIDIA NIM integration with structured output and retry logic
- Schema validation at every boundary — no free-text that can't be evaluated
- Deterministic signal computation BEFORE AI inference (not just a prompt wrapper)
- Working backtest with technical indicators and equity curves
- Full explainability: signal weights, weak points, thesis invalidators

## Originality
- 6-regime classification system, not binary bull/bear
- Multi-signal fusion: momentum + sentiment + volatility + derivatives + on-chain
- Critique loop that catches and fixes weak strategies before output
- Deterministic fallback path ensures the skill never produces empty output
- Regime-specific synthetic data generation for meaningful backtests

## Relevance
- Directly targets Track 2: Strategy Skills
- CMC Skill as the core deliverable — not a trading bot, not a portfolio tracker
- Uses CMC Agent Hub data as the primary data source
- Output is immediately useful for traders and agent builders
- Backtestable strategy spec is the exact Track 2 requirement

## Demo Quality
- One-click preset scenarios (BTC, ETH, SOL, BNB, DOGE)
- Instant regime classification and strategy generation
- Visible explainability with signal weight bars
- Backtest results with equity curve visualization
- Clean dark UI with sharp typography
- Architecture page explaining the full system to judges
